// resource.h: IDs for Win32 resources embedded in the executable.
// The .rc file references these symbols; main.cpp / window.cpp use them
// at runtime via LoadIcon / FindResource.
//
// Keep IDs above 100 to avoid collisions with system-defined values.

#pragma once

#define IDI_APP_ICON 101
